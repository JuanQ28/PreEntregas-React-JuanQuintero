import { NavLink } from "react-router-dom";

const Brand = () => {
    return (
        <NavLink className="navbar-brand" to="/">
            ArmyBody
        </NavLink>
    );
};

export default Brand;